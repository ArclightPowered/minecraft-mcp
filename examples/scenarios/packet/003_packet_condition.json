{
  "name": "packet condition property",
  "tags": ["packet", "world"],
  "steps": [
    {
      "tool": "mc.client.packet.recording.start",
      "args": {
        "clear": true,
        "maxPackets": 200,
        "direction": "both"
      }
    },
    {
      "tool": "mc.client.condition.wait",
      "args": {
        "condition": "packet.recording == true",
        "timeoutMs": 1000
      },
      "expect": {
        "result.matched": true
      }
    },
    {
      "tool": "mc.client.packet.recording.status",
      "args": {},
      "expect": {
        "result.recording": true
      }
    },
    {
      "tool": "mc.client.packet.recording.stop",
      "args": {}
    }
  ]
}
