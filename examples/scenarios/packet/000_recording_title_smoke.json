{
  "name": "packet recording title smoke",
  "tags": ["packet", "smoke"],
  "steps": [
    {
      "tool": "mc.client.packet.recording.start",
      "args": {
        "clear": true,
        "maxPackets": 20,
        "direction": "both"
      },
      "expect": {
        "result.recording": true
      }
    },
    {
      "tool": "mc.client.condition.wait",
      "args": {
        "condition": "packet.recording == true",
        "timeoutMs": 1000
      },
      "expect": {
        "result.matched": true
      }
    },
    {
      "tool": "mc.client.packet.recording.stop",
      "args": {},
      "expect": {
        "result.recording": false
      }
    }
  ]
}
